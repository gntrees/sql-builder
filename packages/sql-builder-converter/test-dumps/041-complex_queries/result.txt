Match: PASS
Input SQL: SELECT u.name, COUNT(o.id) as order_count FROM users u LEFT JOIN orders o ON u.id = o.user_id WHERE u.active = true GROUP BY u.name HAVING COUNT(o.id) > 0 ORDER BY order_count DESC
Output SQL: SELECT u.name, COUNT(o.id) AS order_count FROM users AS u LEFT JOIN orders AS o ON u.id = o.user_id WHERE u.active = true GROUP BY u.name HAVING count(o.id) > 0 ORDER BY order_count DESC

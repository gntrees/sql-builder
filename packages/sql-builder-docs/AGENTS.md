- install shadcn component using command. dont try to create it yourself. it will be created by the command itself.

```bash
pnpm dlx --bun shadcn@latest list @shadcn
bunx shadcn-ui@latest add {component-name}
```

- dont run bun command